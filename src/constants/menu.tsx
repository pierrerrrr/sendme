type FieldProps = {
    label: string
    id: string
}

type SideBarProps = {
    icon: React.ReactNode;
} & FieldProps
export const SIDEBAR_MENU: SideBarProps[] = [
    {
        id: uuid
    }
]

// NEXT STEP: INSTALL NPM INSTALL UUID